// Entrada `@drinks-on-chain/mocks/fixtures`: los JSON del ERP ya tipados y los usuarios de demo.
// Los archivos crudos también se exportan en `@drinks-on-chain/mocks/fixtures/erp/<archivo>.json`.

export { DEMO_PASSWORD, demoUsers, erpFixtures, type DemoUser, type ErpFixtures } from './erp/fixtures'
